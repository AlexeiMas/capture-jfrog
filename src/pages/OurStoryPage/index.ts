import { connect } from 'react-redux';
import { OurStoryPage as Self } from './OurStoryPage';

const mapStateToProps = () => ({
});

const mapDispatchToProps = {

};

const OurStoryPage = connect(
  mapStateToProps,
  mapDispatchToProps
)(Self);

export default OurStoryPage;
