import { connect } from 'react-redux';

import { RegistrationOverBody as Self } from './RegistrationOverBody';

const mapDispatchToProps = {
};

export const RegistrationOverBody = connect(
  null,
  mapDispatchToProps
)(Self);
